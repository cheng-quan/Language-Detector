/**
 *
 * by Zeying Peng, 2011-09-13
 */

#ifndef __Trigram_h_
#define __Trigram_h_

#include <string>
#include "Trie.h"
#include "Model.h"

class Trigram : public Model
{
public:
	Trigram( );
	virtual ~Trigram( );

	virtual float similarity( const ::std::string& iMessage );
	
	static bool train( const char* iTrainFileName, const char* iModelFileName );

	bool load( const char* iFileName );
	bool save( const char* iFileName );

private:
	// for no copy
	Trigram( const Trigram& tr );
	const Trigram& operator=( const Trigram& tr );

	Trie	m_trie;
};

#endif

